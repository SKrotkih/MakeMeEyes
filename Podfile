project 'MasterFace.xcodeproj'
platform :ios, '10.0'
use_frameworks!

# Pods for MasterFace
target 'MasterFace' do
  pod 'Fabric'                              # crashes tracker
  pod 'Crashlytics'                         # crashes tracker
end
